{
  "name": "mvp-bonus-tma",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c auto \"npm:dev --workspace=server\" \"npm:dev --workspace=client\""
  },
  "workspaces": [
    "server",
    "client"
  ],
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}