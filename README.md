# MVP Bonus TMA

## Запуск
```bash
cd server && cp .env.example .env && cd ..
npm install
cd server && npm install && cd ..
cd client && npm install && cd ..
npm run dev
```

Открой Mini App в Telegram, чтобы `initData` был доступен фронту.
В `.env` укажи BOT_TOKEN и ADMIN_TG_IDS.
